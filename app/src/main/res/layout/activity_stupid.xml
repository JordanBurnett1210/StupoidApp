<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".StupidActivity"
    android:id="@+id/backgroundLayout">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="CHANGE SCREEN COLOR"
        android:id="@+id/colorChangeButton"
        android:enabled="false"
        android:longClickable="false"
        android:layout_gravity="center_vertical"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="CHANGE THE SCREEN"
        android:id="@+id/swapScreenButton"
        android:enabled="false"
        android:longClickable="false"
        android:layout_gravity="center_vertical"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="My color changing app!"
        android:id="@+id/textView"
        android:layout_alignParentTop="true"
        android:layout_marginTop="81dp"
        android:layout_alignRight="@+id/colorChangeButton"
        android:layout_alignEnd="@+id/colorChangeButton"
        android:layout_alignLeft="@+id/colorChangeButton"
        android:layout_alignStart="@+id/colorChangeButton"
        android:layout_above="@+id/colorChangeButton" />


</RelativeLayout>
